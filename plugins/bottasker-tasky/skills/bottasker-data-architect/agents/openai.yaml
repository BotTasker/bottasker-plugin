interface:
  display_name: "BotTasker Data Architect"
  short_description: "Design Base de datos module (Base de datos service), models, relations, and tables."
  brand_color: "#2563EB"
  default_prompt: "Design the Base de datos (Base de datos) and records for a BotTasker app."
policy:
  allow_implicit_invocation: true
