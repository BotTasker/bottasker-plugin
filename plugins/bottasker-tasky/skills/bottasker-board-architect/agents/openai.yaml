interface:
  display_name: "BotTasker Board Architect"
  short_description: "Design boards, column alerts, card details, widgets, sharing, roles, and security."
  brand_color: "#2563EB"
  default_prompt: "Design a BotTasker board with data source, columns, per-user alerts, detail view, widgets, automations, and secure sharing."
policy:
  allow_implicit_invocation: true
