interface:
  display_name: "BotTasker Router"
  short_description: "Route BotTasker tasks to the right MCP workflow."
  brand_color: "#2563EB"
  default_prompt: "Use BotTasker MCP to inspect context and route the task."
policy:
  allow_implicit_invocation: true

